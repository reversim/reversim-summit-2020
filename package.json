{
  "name": "reversim-summit-2018",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server",
    "dev": "node dev.js",
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "heroku-postbuild": "cd client && npm install --production=false && npm build"
  },
  "engines": {
    "node": ">=8.11.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "nodemon": "^1.18.3"
  },
  "devDependencies": {
    "cloudinary": "^1.11.0",
    "dotenv": "^6.0.0",
    "eslint-plugin-node": "^7.0.1",
    "mongodb": "^3.1.1"
  }
}
