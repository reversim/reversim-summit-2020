{
  "name": "reversim-summit-2018",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server",
    "dev": "node dev.js",
    "postinstall": "cd server && yarn && cd ../client && yarn",
    "heroku-postbuild": "cd client && yarn --production=false && yarn build"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "nodemon": "^1.12.5"
  }
}
