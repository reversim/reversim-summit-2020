{
  "name": "reversim-summit-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server",
    "dev": "node dev.js",
    "postinstall": "cd server && yarn install && cd ../client && yarn install",
    "heroku-postbuild": "cd client && yarn install --production=false && yarn run build"
  },
  "engines": {
    "node": ">=12.13.0"
  },
  "dependencies": {
    "@fortawesome/free-brands-svg-icons": "^5.7.2",
    "axios": "^0.18.1",
    "cloudinary": "^1.13.2",
    "mongodb": "^3.5.4",
    "nodemon": "^2.0.2",
    "react-stepzilla": "^6.0.2"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.6",
    "dotenv": "^6.0.0",
    "eslint-plugin-node": "^7.0.1"
  }
}
